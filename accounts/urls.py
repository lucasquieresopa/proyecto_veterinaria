from django.urls import path
#from .views import SignUpView


urlpatterns = [
    #path('signup/', SignUpView.as_view(), name='signup'),   #/accounts/signup muestra la view de signup (la 
                                                            #cual es un template .html linkeado a una clase viewSignup)
]
